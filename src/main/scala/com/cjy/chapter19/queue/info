
* 数组存储队列方式：
*   1、指定队列大小不重复使用，相当于一次性使用
*   2、每次出队一个元素，数组整体向前移动一步；入队根据坐标，快，出队需要迁移，慢
*   3、循环队列，空间多次复用，使用队列时不能将队列填满，需要空出一个位置（推荐）



链表实现队列：链表实现队列，出队快，入队慢
    1、单向队列：移除节点需要临时节点
    2、双向队列：移除节点不需要临时节点
    3、循环对了：特殊场景使用，如约瑟夫问题：数数退出